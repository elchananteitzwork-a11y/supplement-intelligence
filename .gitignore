# ─── macOS ───────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
Icon?
ehthumbs.db
Thumbs.db

# ─── Editors & IDEs ──────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
*~

# ─── Environment & Secrets ───────────────────────────────
.env
.env.local
.env.production
.env.staging
# .env.example is intentionally committed as documentation
*.pem
*.key
secrets.json
credentials.json

# ─── Node / npm (if tooling is added later) ──────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# ─── Python (if tooling is added later) ──────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# ─── Plugin Runtime Output Files ─────────────────────────
# These are generated at analysis time — not source files.
# Commit them intentionally if you want to version saved analyses.
opportunity-ranking.md
product-opportunity-report.md
product-ranking.json
brand-builder-report.md
investment-analysis.md

# ─── Keepa Integration ───────────────────────────────────
# API cache — do not commit (contains raw product data, rebuilt on each run)
keepa_cache/
# keepa-report.json is tracked — it serves as the output schema for Phase 1

# ─── Logs & Temp ─────────────────────────────────────────
*.log
*.tmp
*.temp
.cache/
